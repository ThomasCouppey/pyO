# Contributing to pyo

1. Create a virtual environment and install dependencies with `pip install -e .[dev]`.
2. Run formatting and linting using `ruff check .` and `black src tests` before committing.
3. Add or update unit/integration tests under `tests/` to cover new behavior.
4. Open a pull request on GitHub describing the feature and linking any design docs in `docs/`.
